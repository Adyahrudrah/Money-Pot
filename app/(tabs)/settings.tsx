import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const Settings = () => {
  return <SafeAreaView className="bg-bg_primary"></SafeAreaView>;
};

export default Settings;
